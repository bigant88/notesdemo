<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NotesDemo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NotesDemo" parent="Base.Theme.NotesDemo" />
    <style name="LargeTextView" parent="TextAppearance.AppCompat.Large">
        <item name="android:textColor">@color/primaryColor</item>
    </style>
    <style name="MediumTextView" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/primaryColor</item>
    </style>
    <style name="SmallTextView" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/black</item>
    </style>
</resources>